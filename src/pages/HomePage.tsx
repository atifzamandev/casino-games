import { FC } from 'react'
import LoginForm from '../components/LoginForm/LoginForm'

const HomePage: FC = () => {
  return (
    <main className="body">
      <LoginForm />
    </main>
  )
}

export default HomePage
